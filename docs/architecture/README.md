# Architecture records

Architecture decisions belong here as focused ADRs. Keep engine/game dependency direction explicit: game code may depend on public engine APIs, while engine crates must not depend on game crates.
